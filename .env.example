# YAHRIA BUSINESS OS — Environment template
# Copy to .env and fill with your own values. NEVER commit the real .env.

# SQLite (dev) — path to the local database file
DATABASE_URL=file:/home/z/my-project/db/custom.db

# Ed25519/HMAC secret used to sign Evidence (INV-007 Event Immutability).
# Generate with: openssl rand -hex 32
EVIDENCE_SIGNING_KEY=replace_me_with_openssl_rand_hex_32
# ⚠ ROTATION : si vous changez cette clé, les signatures Evidence existantes ne
# se vérifient plus (contenu intact, INV-008 passe en « ROTATION DE CLÉ »).
# Réparez avec l'action de gouvernance OWNER/ADMIN :
#   POST /api/v1/governance { "action": "RESEAL_EVIDENCE", "allOrgs": true }
# Le re-scellement recalcule les signatures sans toucher aux hashs/chaînage et
# REFUSE toute chaîne dont le contenu est altéré (falsification réelle).

# SEC-003 — 2FA progressive lockout waves: 1 = OWNER+CFO only, 2 = +ADMIN+ACCOUNTANT,
# 3 = all roles (default). Unenrolled users of active waves can only access auth routes.
# YAHRIA_MFA_WAVE=3

# SEC-003 — 2FA demo mode. TOTP stays REAL in every mode; this only controls
# code-entry assistance for demonstration environments:
#   assist (default) — current TOTP code is displayed & auto-filled in the UI
#                      (login step 2 + enrollment): full flow demoable without
#                      an authenticator app.
#   off              — 2FA fully disabled: password-only login, MFA wall never fires.
#   strict           — production: no assistance at all.
# YAHRIA_DEMO_2FA=assist
