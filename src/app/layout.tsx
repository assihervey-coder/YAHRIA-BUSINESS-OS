import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "YAHRIA BUSINESS OS V1 — The Intelligent Operating System for African Business",
  description:
    "Système d'exploitation métier intelligent : DATA → GRAPH → INTELLIGENCE → DÉCISION → POLICY → AGENTS → EXÉCUTION → EVIDENCE. OHADA, Mobile Money, agents sous gouvernance.",
  keywords: ["YAHRIA", "Business OS", "OHADA", "Mobile Money", "Agents IA", "Afrique"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="fr" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
